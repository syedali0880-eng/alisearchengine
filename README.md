# alisearchengine
alisearchengine works like a mini search engine which will scan through all the files within sub folders and give you results . 
this needs a python to be installed in the PC and you need to run start_server.bat to start the instance . This can be access from the url http://localhost:8000/
